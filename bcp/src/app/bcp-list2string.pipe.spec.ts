import { BcpList2stringPipe } from './bcp-list2string.pipe';

describe('BcpList2stringPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BcpList2stringPipe();
    expect(pipe).toBeTruthy();
  });
});
